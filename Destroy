using UnityEngine;

public class DestroyOnFall : MonoBehaviour
{
    public float fallY = -10f;

    void Update()
    {
        if (transform.position.y < fallY)
        {
            Destroy(gameObject);
        }
    }
}
